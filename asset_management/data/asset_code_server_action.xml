<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="0">
        <!-- Server Action: Reassign ALL Asset Codes by type -->
        <record id="action_reassign_asset_codes" model="ir.actions.server">
            <field name="name">Reassign Asset Codes by Type</field>
            <field name="model_id" ref="asset_management.model_asset_asset"/>
            <field name="binding_model_id" ref="asset_management.model_asset_asset"/>
            <field name="binding_view_types">list,form</field>
            <field name="state">code</field>
            <field name="code">
action = model.action_reassign_all_asset_codes()
            </field>
        </record>
    </data>
</odoo>
